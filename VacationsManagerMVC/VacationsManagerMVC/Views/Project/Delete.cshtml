@model VacationsManagerMVC.ViewModels.ProjectDetailsVM

@{
    ViewBag.Title = "Delete Project";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewBag.Title</title>
    <link href="https://cdn.jsdelivr.net/npm/bootswatch@5.3.2/dist/lux/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card shadow-sm">
                    <div class="card-header bg-danger text-white">
                        <h2>Delete Project</h2>
                    </div>
                    <div class="card-body">
                        <h5 class="text-center text-danger">Are you sure you want to delete this project?</h5>
                        <hr />
                        <dl class="row">
                            <dt class="col-sm-4">Project Name:</dt>
                            <dd class="col-sm-8">@Model.Name</dd>

                            <dt class="col-sm-4">Description:</dt>
                            <dd class="col-sm-8">@Model.Description</dd>

                            <dt class="col-sm-4">Teams Involved:</dt>
                            <dd class="col-sm-8">
                                @if (Model.Teams.Any())
                                {
                                    <ul>
                                        @foreach (var team in Model.Teams)
                                        {
                                            <li>@team.Name</li>
                                        }
                                    </ul>
                                }
                                else
                                {
                                    <span>No teams assigned.</span>
                                }
                            </dd>
                        </dl>
                        <hr />
                        <div class="text-center">
                            @using (Html.BeginForm("Delete", "Project", FormMethod.Post))
                            {
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="id" value="@Model.Id" />
                                <button type="submit" class="btn btn-danger">Delete</button>
                                <a href="@Url.Action("List", "Project")" class="btn btn-secondary">Cancel</a>
                            }
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
